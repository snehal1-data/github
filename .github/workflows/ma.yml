    name: Run python Pograms

    on:
      push:  
       branches:
         -main

    jobs:
     run-all:
      runs-on: ubuntu-latest

      steps:
       #checkout repository code
       - name: Checkout code
         uses: actions/checkout@v4

       # set up python
       - name: Set up Python
         uses: actions/setup-python@v5
         with:
           python-version: '3.10'

       #Run first python-script
       - name: Run sneha1.py
         run: sneha1.py
         
     
